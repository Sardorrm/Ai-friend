# Ai-friend

Run the bot locally:

```bash
cd /workspaces/Ai-friend
export OPENAI_API_KEY="your-api-key"
python3 run_ai_friend.py
```

If no API key is set, the app uses a simple built-in fallback response system.